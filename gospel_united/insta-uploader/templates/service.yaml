apiVersion: v1
kind: Service
metadata:
  name: {{ include "insta-uploader.fullname" . }}
  labels:
    {{- include "insta-uploader.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insta-uploader.selectorLabels" . | nindent 4 }}
