apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "insta-uploader.fullname" . }}-test-connection"
  labels:
    {{- include "insta-uploader.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "insta-uploader.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
